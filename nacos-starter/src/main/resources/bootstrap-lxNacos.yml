spring:
  cloud:
    nacos:
      config:
        group: LX
        server-addr: nacos:8848
        namespace: namespace
        file-extension: yaml
      discovery:
        group: LX
        server-addr: nacos:8848
        namespace: namespace